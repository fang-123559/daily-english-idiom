---
name: daily-english-idiom
description: 每日英语俗语推送模板。生成SVG可视化卡片+结构化文字，含含义、例句、来源、记忆提示。用于每日英语俗语推荐任务。触发词：每日英语俗语、每日一句英语、idiom、俗语推送、英语俗语卡片。
agent_created: true
---

# 每日英语俗语推送模板

每天推荐一条通俗的、英语文化中日常使用的、会在英语口语或书面表达里出现的英语俗语，优先选取生活和职场中常见的俗语。

## 执行流程（必须严格遵守）

### 第一步：读取历史记录（避免重复）
1. 读取项目根目录下的 `memory.md` 文件
2. 找到「历史推荐」表格，获取所有已推送过的俗语列表
3. **严禁**推荐表格中已存在的任何俗语

**失败模式编码**：
- 如果 memory.md 文件不存在 → 创建新文件，初始化「历史推荐」表格（可参考 `templates/memory-template.md`）
- 如果「历史推荐」表格格式损坏 → 备份原文件，重建表格
- 如果读取文件超时（>5秒） → 跳过历史检查，从常用俗语中随机选择，记录警告

### 第二步：选择新俗语
- 从常见英语俗语库中选择一条**未在历史记录中**的俗语
- 优先选取：日常生活和职场中高频使用的俗语
- 确保俗语有清晰的含义、来源和记忆点

**失败模式编码**：
- 如果所有俗语都已推送过 → 清空历史记录，从头开始新一轮推送，记录警告
- 如果俗语含义不清晰 → 跳过该俗语，选择下一条
- 如果俗语有冒犯性或争议 → 立即跳过，选择更中性的俗语

### 第三步：生成推送内容
按以下**完整格式**输出：

#### 第一部分：SVG 可视化卡片
使用 `show_widget` 生成，严格按以下参数和样式：

**失败模式编码**：
- 如果 show_widget 工具不可用 → 降级为纯文本输出，记录警告
- 如果 SVG 渲染失败（超时/错误） → 重试一次，仍失败则降级为纯文本
- 如果俗语过长（>20字符） → 缩小字号或拆分为两行显示，确保不溢出卡片

**viewBox**: "0 0 680 520"

**卡片主体**: 
```svg
<rect x="40" y="20" width="600" height="480" rx="16" fill="#F8F7FE" stroke="#534AB7" stroke-width="1.5"/>
```

**日期胶囊**: 
```svg
<rect x="70" y="45" width="100" height="28" rx="14" fill="#534AB7"/>
<text x="120" y="64" font-family="sans-serif" font-weight="500" font-size="13" fill="#FFFFFF" text-anchor="middle">M月D日</text>
```

**标题**: 
```svg
<text x="340" y="110" font-family="sans-serif" font-weight="600" font-size="28" fill="#26215C" text-anchor="middle">{俗语}</text>
```

**音标**: 
```svg
<text x="340" y="140" font-family="sans-serif" font-weight="400" font-size="14" fill="#888780" text-anchor="middle">/{音标}/</text>
```

**分隔线**: 
```svg
<line x1="70" y1="165" x2="610" y2="165" stroke="#534AB7" stroke-width="1" stroke-opacity="0.3"/>
```

**标签**（统一尺寸 x="100" y="{见下}" width="60" height="26" rx="6"，文字 font:500 12px #FFFFFF text-anchor:middle，x="130"）:
- 含义：fill="#7F77DD"，y="203"
- 例句：fill="#0F6E56"，y="248"
- 来源：fill="#BA7517"，y="313"
- 记忆：fill="#D85A30"，y="378"

**页脚**: 
```svg
<text x="340" y="475" font-family="sans-serif" font-weight="400" font-size="12" fill="#888780" text-anchor="middle">每日一俗语 · 让英语融入生活</text>
```

#### 文本渲染规范（重要）

由于SVG文本不支持自动换行，**所有多行文本必须拆分为多个 `<text>` 元素**：
- 每行使用独立的 `<text x="145" y="起始Y">内容</text>`
- 行间距：16px（如 y="222", y="238", y="254"）
- 例句中的**俗语部分**使用 `<tspan font-weight="700" fill="#0F6E56">` 高亮
- 每段内容最多3行，超出则精简文字

#### 第二部分：卡片下方结构化文字

SVG 卡片输出后，立即输出以下结构化文字（Markdown 格式）：

```markdown
🗣️ **今日英语俗语**

# **"{俗语}"**
/{音标}/

---

📖 **含义**

字面意思是"..."，引申为...。常用来...。日常生活和职场中都非常高频。

---

💬 **例句**

**例句一（场景描述）**
> "{英文例句，俗语部分用 **加粗** 标注}。"
> （中文翻译。）

**例句二（不同场景）**
> "{英文例句，俗语部分用 **加粗** 标注}。"
> （中文翻译。）

---

🌍 **俗语来源**

这句话起源于 **{时期/背景}**。...最早在 {年份} 左右的文献中就有记载。

---

🧠 **记忆小提示**

想象{生动的场景画面}来帮助记忆...所以 **{关键词拆解逻辑}**。

---

💡 **关键词联想：**{关键词} = {含义}，{使用场景提示}。

---

📅 **{YYYY年M月D日}** · 每日一俗语
```

### 第四步：更新历史记录
1. 将本次推荐的俗语、日期、中文含义添加到 memory.md 的「历史推荐」表格
2. 在「执行日志」中记录本次执行情况
3. 确保文件编码为 UTF-8

**失败模式编码**：
- 如果写入 memory.md 失败 → 重试一次，仍失败则记录到临时文件，下次执行时合并
- 如果文件编码不是 UTF-8 → 转换为 UTF-8 后再写入
- 如果磁盘空间不足 → 压缩历史记录，删除30天前的记录

## 输出要求
- 俗语部分用 **加粗** 标注，帮助快速定位
- 例句至少 2 条，涵盖不同使用场景
- 例句中俗语用 **加粗**，中文翻译用括号包裹
- 来源需注明时期、背景、最早文献记载时间
- 记忆小提示需要包含一个生动的场景想象 + 关键词拆解逻辑
- 关键词联想：一句话点睛，关联用法场景
- 日期使用"YYYY年M月D日"格式

## 反例与黑名单（不要做什么）

| # | 反模式 | 为什么不要做 | 替代做法 |
|---|--------|--------------|----------|
| 1 | **推荐已存在的俗语** | 重复推送会让用户失去兴趣 | 严格执行去重机制，读取历史记录后再选择 |
| 2 | **推荐过时/生僻的俗语** | 用户无法在实际场景中使用 | 优先选取近10年仍在使用的俗语 |
| 3 | **推荐有冒犯性的俗语** | 可能引起用户不适或投诉 | 选择中性、积极的俗语，避免涉及种族、性别、宗教等敏感话题 |
| 4 | **编造俗语来源** | 虚假信息会损害信任 | 只使用有据可查的来源，无法确认时标注"来源待考证" |
| 5 | **例句过于复杂** | 用户难以理解和记忆 | 例句控制在15个单词以内，使用简单句式 |
| 6 | **记忆提示过于抽象** | 用户无法形成生动印象 | 使用具体场景+关键词拆解，让用户能想象出画面 |
| 7 | **SVG卡片文字溢出** | 影响视觉效果和阅读体验 | 控制每行文字长度，超出时精简或换行 |
| 8 | **忽略日期格式** | 日期格式不一致影响专业性 | 严格使用"YYYY年M月D日"格式 |
| 9 | **不记录执行日志** | 无法追踪问题和优化 | 每次执行后必须记录到memory.md |
| 10 | **跳过用户确认** | 可能生成不符合用户期望的内容 | 测试prompt设计后必须展示给用户确认 |

**触发场景**：每轮执行前对照本表一次。任一反模式命中 → 立即修正。

---

## 异常处理（失败模式编码汇总）

| 场景 | 触发条件 | 处理动作 |
|------|----------|----------|
| 历史记录文件缺失 | memory.md 不存在 | 创建新文件，初始化表格 |
| 历史记录格式损坏 | 表格格式不正确 | 备份原文件，重建表格 |
| 读取超时 | 读取文件 >5秒 | 跳过历史检查，随机选择俗语 |
| 所有俗语已推送 | 历史记录中无可用俗语 | 清空历史，开始新一轮 |
| 俗语含义不清晰 | 无法解释俗语含义 | 跳过该俗语，选择下一条 |
| 俗语有冒犯性 | 俗语涉及敏感话题 | 立即跳过，选择中性俗语 |
| SVG渲染失败 | show_widget 超时/错误 | 重试一次，仍失败降级纯文本 |
| 俗语过长 | 俗语 >20字符 | 缩小字号或分两行显示 |
| 写入失败 | 无法写入 memory.md | 重试一次，仍失败记录到临时文件 |
| 编码错误 | 文件编码不是 UTF-8 | 转换为 UTF-8 后写入 |
| 磁盘空间不足 | 写入失败 | 压缩历史记录，删除旧记录 |

**原则**：异常先记录警告，再按规则处理；绝不静默跳过或静默失败。

---

## 去重机制说明

**俗语来源：** 模型训练知识中的常见英语俗语（无本地文件）。

**去重依赖：** 项目根目录下 `memory.md` 文件中的「历史推荐」表格。

每次执行时：
1. 读取 memory.md 中的「历史推荐」表格
2. 获取所有已推送过的俗语列表
3. 从模型知识中选择一条**未在表格中**的俗语
4. 推荐后，将新俗语添加到表格

这样可以确保每次推送都不重复，且历史记录会自动更新。
